apiVersion: v1
kind: Service
metadata:
  name: {{ include "guhatek-onegt.fullname" . }}
  labels:
    {{- include "guhatek-onegt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "guhatek-onegt.selectorLabels" . | nindent 4 }}
